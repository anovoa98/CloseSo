import 'package:flutter/material.dart';
//tab configuraciones
class Acercade extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: new AppBar(
        title: new Text('Nosotros...'),
      ),
      body: new Center(
        child: new Text('Estamos aprendiendo....'),
      ),
    );
  }
}